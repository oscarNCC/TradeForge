# Netlify config for TradeForge Frontend
# Used when repo root is the repo and base is TradeForge-FrontEnd

[build]
  command = "npm run build"
  publish = "dist"

# SPA: all routes fallback to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
